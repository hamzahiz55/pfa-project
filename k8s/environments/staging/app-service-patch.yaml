apiVersion: v1
kind: Service
metadata:
  name: aws-s3-manager-service
  namespace: aws-s3-manager
spec:
  type: NodePort
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
      nodePort: 30081